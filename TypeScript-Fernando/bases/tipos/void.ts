(() => {

    function callBatman():void { // La funcion void retorna undefined, para asignat el tipo de dato que retorna una funcion se le asigna con dos puntos despues de los parentesis ():tipo_de_dato
        
    }
    const a = callBatman();
    console.log(a);
})()